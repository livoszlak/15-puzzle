export const TILE_COUNT: number = 16;
export const GRID_SIZE: number = 4;
export const BOARD_SIZE: number = 320;

export const TILE_WIDTH: number = Math.round(BOARD_SIZE / GRID_SIZE);
export const TILE_HEIGHT: number = Math.round(BOARD_SIZE / GRID_SIZE);
