import { Constants } from "../types";

export const constants: Constants = {
  TILE_COUNT: 16,
  GRID_SIZE: 4,
  BOARD_SIZE: 320,
  TILE_WIDTH: Math.round(320 / 4),
  TILE_HEIGHT: Math.round(320 / 4),
};
